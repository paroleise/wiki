.row
  - unless be_diff?(machine.manufacturer, previous.manufacturer)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.manufacturer")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.manufacturer, machine.manufacturer)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.manufacturer, previous.manufacturer)

  - unless be_diff?(machine.introduction_date, previous.introduction_date)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.introduction_date")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.introduction_date, machine.introduction_date)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.introduction_date, previous.introduction_date)

  - unless be_diff?(machine.archetype, previous.archetype)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.archetype")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.archetype, machine.archetype)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.archetype, previous.archetype)


  - unless be_diff?(machine.spec, previous.spec)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.spec")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.spec, machine.spec)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.spec, previous.spec)


  - unless be_diff?(machine.judgment, previous.judgment)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.judgment")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.judgment, machine.judgment)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.judgment, previous.judgment)

  - unless be_diff?(machine.judgment, previous.judgment)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.details")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.details, machine.details)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.details, previous.details)

  - unless be_diff?(machine.overview, previous.overview)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.overview")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.overview, machine.overview)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.overview, previous.overview)

  - unless be_diff?(machine.feature, previous.feature)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.feature")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.feature, machine.feature)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.feature, previous.feature)

  - unless be_diff?(machine.evaluation_point, previous.evaluation_point)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.evaluation_point")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.evaluation_point, machine.evaluation_point)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.evaluation_point, previous.evaluation_point)

  - unless be_diff?(machine.pros_and_cons, previous.pros_and_cons)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.pros_and_cons")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.pros_and_cons, machine.pros_and_cons)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.pros_and_cons, previous.pros_and_cons)

  - unless be_diff?(machine.problem, previous.problem)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.problem")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.problem, machine.problem)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.problem, previous.problem)

  - unless be_diff?(machine.conclusion, previous.conclusion)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.conclusion")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.conclusion, machine.conclusion)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.conclusion, previous.conclusion)

  - unless be_diff?(machine.degression, previous.degression)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.degression")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.degression, machine.degression)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.degression, previous.degression)

  - unless be_diff?(machine.other, previous.other)
    .col-sm-12
      hr
      strong = t("activerecord.attributes.machine.other")
    .col-sm-6
      p.diff-changes-head 編集前の内容
      .diff-old == decorate_diff(previous.other, machine.other)
    .col-sm-6
      p.diff-changes-head = "#{current ? '現在' : '編集後'}の内容"
      .diff-new == decorate_diff(machine.other, previous.other)
